# References

This directory stores imported third-party design and code references.

Current contents:

- `design-md/` for imported design reference material
- `liquid-glass-react/` for liquid glass component reference code

Rule:

- references are not production source of truth
- if code is adopted into the main product, extract it intentionally into `apps/` or `packages/`
