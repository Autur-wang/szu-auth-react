# Packages

This directory is reserved for shared layers extracted from apps and services.

Use it for code that should be reusable, stable, and contract-driven.

Initial target package areas:

- `contracts/`
- `config/`
- `ui/`
- `utils/`

