# Contracts Package

Use this area for shared contract definitions:

- HTTP API schemas
- Electron IPC payload shapes
- task state and result shapes

This package should eventually align with `harness/contracts/`.

