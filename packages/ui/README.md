# UI Package

Use this area for shared UI primitives that are stable enough to reuse across apps.

Do not move one-off page code here too early.

Only extract:

- stable components
- shared patterns
- common status and layout primitives

